<!-- File: HMI_Client/Visual/TelemetryGraphsView.xaml -->
<UserControl x:Class="HMI_Client.Visual.TelemetryGraphsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="220" d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- График акселерометра -->
        <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Margin="0,0,2,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="8,4">
                    <TextBlock Text="📊 Акселерометр (m/s²)" FontSize="11" FontWeight="SemiBold" Foreground="#1976D2"/>
                </Border>
                <lvc:CartesianChart x:Name="AccelChart" Grid.Row="1" Margin="4"/>
            </Grid>
        </Border>

        <!-- График гироскопа -->
        <Border Grid.Column="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Margin="2,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="8,4">
                    <TextBlock Text="📊 Гироскоп (rad/s)" FontSize="11" FontWeight="SemiBold" Foreground="#D32F2F"/>
                </Border>
                <lvc:CartesianChart x:Name="GyroChart" Grid.Row="1" Margin="4"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>